**/**
!appsscript.json
!*.js
!*.html

# check_docs.js is Node, not Apps Script: it requires fs and path, which do not exist
# there, and it needs the filesystem to read the .md files it checks. Without this line
# the whitelist above sweeps it up as a *.js and clasp push installs permanently broken
# code into the project.
check_docs.js
